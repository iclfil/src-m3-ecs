namespace MatchThree.ECS.Modules
{
    public enum NameModule
    {
        Starting,
        FindMatches,
        Finishing,
        Matches,
        NoMatches,
        Destroying,
        Collapse
    }
}